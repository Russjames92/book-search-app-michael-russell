import React from 'react';

function Filter() {
    return (
        <div className="filter-bar-container">
            <select className="print-type"></select>
            <select className="book-type"></select>
        </div>
    );
}

export default Filter